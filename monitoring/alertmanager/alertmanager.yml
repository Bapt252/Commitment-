# Configuration AlertManager pour le projet Commitment
global:
  # Paramètres SMTP (à configurer selon vos besoins)
  smtp_smarthost: 'localhost:587'
  smtp_from: 'alertmanager@commitment.local'
  smtp_auth_username: ''
  smtp_auth_password: ''
  smtp_require_tls: false

# Modèles d'alertes
templates:
  - '/etc/alertmanager/templates/*.tmpl'

# Routage des alertes
route:
  group_by: ['alertname', 'cluster', 'service']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'default-webhook'
  routes:
    # Alertes critiques - notification immédiate
    - match:
        severity: critical
      receiver: 'critical-alerts'
      group_wait: 10s
      repeat_interval: 5m
    
    # Alertes d'avertissement - notification groupée
    - match:
        severity: warning
      receiver: 'warning-alerts'
      group_wait: 30s
      repeat_interval: 30m

# Destinataires des alertes
receivers:
  # Webhook par défaut (peut être connecté à Slack, Discord, etc.)
  - name: 'default-webhook'
    webhook_configs:
      - url: 'http://localhost:5001/alert-webhook'
        send_resolved: true
        http_config:
          basic_auth:
            username: 'alert-user'
            password: 'alert-password'

  # Alertes critiques - multiple channels
  - name: 'critical-alerts'
    # Email
    email_configs:
      - to: 'admin@commitment.local'
        subject: '🚨 ALERTE CRITIQUE: {{ .GroupLabels.alertname }}'
        body: |
          {{ range .Alerts }}
          **Alerte:** {{ .Annotations.summary }}
          **Description:** {{ .Annotations.description }}
          **Sévérité:** {{ .Labels.severity }}
          **Service:** {{ .Labels.job }}
          **Instance:** {{ .Labels.instance }}
          **Heure:** {{ .StartsAt }}
          {{ end }}
        headers:
          Priority: 'high'
    
    # Webhook pour notifications instantanées
    webhook_configs:
      - url: 'http://localhost:5001/critical-webhook'
        send_resolved: true
        title: 'Alerte Critique - {{ .GroupLabels.alertname }}'
        text: |
          {{ range .Alerts }}
          🚨 {{ .Annotations.summary }}
          
          📋 **Détails:**
          - Service: {{ .Labels.job }}
          - Instance: {{ .Labels.instance }}
          - Sévérité: {{ .Labels.severity }}
          
          📝 **Description:** {{ .Annotations.description }}
          ⏰ **Heure:** {{ .StartsAt }}
          {{ end }}

  # Alertes d'avertissement
  - name: 'warning-alerts'
    email_configs:
      - to: 'team@commitment.local'
        subject: '⚠️ ALERTE: {{ .GroupLabels.alertname }}'
        body: |
          {{ range .Alerts }}
          **Alerte:** {{ .Annotations.summary }}
          **Description:** {{ .Annotations.description }}
          **Service:** {{ .Labels.job }}
          **Instance:** {{ .Labels.instance }}
          **Heure:** {{ .StartsAt }}
          {{ end }}

# Inhibition des alertes (évite le spam)
inhibit_rules:
  # Si un service est down, ne pas envoyer d'alertes sur ses métriques
  - source_match:
      severity: 'critical'
      alertname: 'ServiceDown'
    target_match:
      severity: 'warning'
    equal: ['instance', 'job']
  
  # Si le CPU est très élevé, ne pas alerter pour la mémoire
  - source_match:
      severity: 'critical'
      alertname: 'HighCPUUsage'
    target_match:
      alertname: 'HighMemoryUsage'
    equal: ['instance']